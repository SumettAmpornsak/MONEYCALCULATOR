# 🚀 คู่มือติดตั้งระบบคำนวณเงินเดือน

## 📋 ขั้นตอนการติดตั้ง

### 1. สร้าง Firebase Project

1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. คลิก "Add project" หรือ "เพิ่มโครงการ"
3. ตั้งชื่อโครงการ (เช่น "salary-calculator")
4. เลือกตั้งค่าตามต้องการและคลิก "Create project"

### 2. เปิดใช้งาน Authentication

1. ในเมนูด้านซ้าย คลิก **Authentication**
2. คลิกแท็บ **Sign-in method**
3. คลิก **Google** แล้วเปิดใช้งาน
4. ใส่อีเมล์สำหรับโครงการ
5. คลิก **Save**

### 3. เปิดใช้งาน Realtime Database

1. ในเมนูด้านซ้าย คลิก **Realtime Database**
2. คลิก **Create Database**
3. เลือก Location (แนะนำ: singapore-southeast1)
4. เลือกโหมด **Test mode** (ชั่วคราว)
5. คลิก **Enable**

### 4. ตั้งค่า Database Rules (สำคัญมาก!)

1. ในหน้า Realtime Database คลิกแท็บ **Rules**
2. **ลบ Rules เดิมทั้งหมด**
3. **คัดลอกเนื้อหาจากไฟล์ `firebase-database-rules.json`** ทั้งหมด
4. วางใน Rules editor
5. คลิก **Publish**

### 5. รับ Firebase Configuration

1. ไปที่ **Project Settings** (คลิกไอคอนเฟืองด้านบนซ้าย)
2. เลื่อนลงไปส่วน **Your apps**
3. คลิกไอคอน **Web** (`</>`)
4. ตั้งชื่อแอป (เช่น "Salary Web App")
5. **ไม่ต้องติ๊ก** Firebase Hosting
6. คลิก **Register app**
7. **คัดลอก firebaseConfig**

ตัวอย่าง:
```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  databaseURL: "https://your-project-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

### 6. แก้ไขไฟล์ HTML

1. เปิดไฟล์ `salary-system.html`
2. ค้นหาบรรทัดที่มี `const firebaseConfig = {`
3. **แทนที่** ค่าทั้งหมดด้วย Config ที่คัดลอกมา
4. บันทึกไฟล์

ตัวอย่าง:
```javascript
// เดิม (ต้องแก้)
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    // ...
};

// แก้เป็น (ใช้ของจริงจาก Firebase)
const firebaseConfig = {
    apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
    authDomain: "salary-calc-12345.firebaseapp.com",
    databaseURL: "https://salary-calc-12345-default-rtdb.asia-southeast1.firebasedatabase.app",
    projectId: "salary-calc-12345",
    storageBucket: "salary-calc-12345.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef1234567890"
};
```

### 7. ทดสอบระบบ

1. เปิดไฟล์ `salary-system.html` ในเบราว์เซอร์ (Chrome, Edge, Firefox)
2. คลิก "เข้าสู่ระบบด้วย Google"
3. เลือกบัญชี Google
4. ทดสอบบันทึกข้อมูลการทำงาน
5. ตรวจสอบรายงาน

---

## 🔒 ความปลอดภัยของ Database Rules

Rules ที่สร้างให้มีความปลอดภัยดังนี้:

### ✅ User Data Protection
- **แต่ละ User อ่าน/เขียนได้เฉพาะข้อมูลของตัวเองเท่านั้น**
- ข้อมูลถูกแยกตาม User ID
- ไม่สามารถเข้าถึงข้อมูลคนอื่นได้

### ✅ Data Validation
- **วันที่**: ต้องเป็นรูปแบบ YYYY-MM-DD
- **กะ**: ต้องเป็น 'morning' หรือ 'night' เท่านั้น
- **ชั่วโมงทำงาน**: 0-24 ชั่วโมง
- **OT**: 0-8 ชั่วโมง
- **สถานะ**: 'work', 'leave', 'absent' เท่านั้น
- **ตัวเลข**: ต้องเป็นบวกและสมเหตุสมผล

### ✅ Logs Protection
- **ทุกคนที่ login แล้วอ่านได้ทั้งหมด** (เพื่อความโปร่งใส)
- **เขียนได้เฉพาะข้อมูลใหม่** (ป้องกันแก้ไข Log)
- **ต้องระบุ email ตรงกับผู้ login**

---

## 📊 โครงสร้างข้อมูลใน Database

```
salary-database/
├── users/
│   └── [userId]/
│       └── workDays/
│           └── [workDayId]/
│               ├── date: "2025-02-14"
│               ├── shift: "morning"
│               ├── workHours: 8
│               ├── otHours: 2.5
│               ├── status: "work"
│               ├── hourlyRate: 50
│               ├── otRate: 1.5
│               ├── workIncome: 400
│               ├── otIncome: 187.5
│               ├── totalIncome: 587.5
│               └── timestamp: 1707897600000
└── logs/
    └── [logId]/
        ├── user: "user@example.com"
        ├── action: "add_work"
        ├── details: "บันทึกการทำงาน วันที่ 2025-02-14"
        ├── timestamp: 1707897600000
        └── date: "14/2/2025, 10:30:00"
```

---

## 🎯 ฟีเจอร์ของระบบ

### 1. Authentication
- ✅ เข้าสู่ระบบด้วย Google Account
- ✅ จัดการ Session อัตโนมัติ
- ✅ ปลอดภัย ไม่ต้องจัดการรหัsผ่าน

### 2. บันทึกการทำงาน
- ✅ เลือกวันที่
- ✅ เลือกกะ (เช้า/ดึก)
- ✅ กำหนดชั่วโมงทำงาน
- ✅ คำนวณ OT (1.5, 2, 2.5, 3 ชม.)
- ✅ ระบุสถานะ (ทำงาน/ลา/ขาด)
- ✅ กำหนดอัตราค่าจ้างได้

### 3. รายงานสรุป
- ✅ จำนวนวันทำงาน/ลา/ขาด
- ✅ ชั่วโมงทำงานรวม + OT
- ✅ รายได้แยกตามประเภท
- ✅ หักประกันสังคม 5%
- ✅ รายได้สุทธิ

### 4. Log การใช้งาน
- ✅ บันทึก Login/Logout
- ✅ บันทึกการเพิ่ม/ลบข้อมูล
- ✅ แสดงผลแบบ Real-time
- ✅ เห็นกิจกรรมของทุกคน (โปร่งใส)

---

## 💡 ตัวอย่างการคำนวณ

### ตัวอย่างที่ 1: กะเช้า + OT 2.5 ชม.
```
วันที่: 10 ก.พ. 2025
กะ: เช้า (07:00-19:00)
ทำงาน: 8 ชั่วโมง
OT: 2.5 ชั่วโมง
อัตรา: 50 บาท/ชม.
OT Rate: 1.5 เท่า

คำนวณ:
- รายได้ทำงาน = 8 × 50 = 400 บาท
- รายได้ OT = 2.5 × 50 × 1.5 = 187.5 บาท
- รวม = 587.5 บาท
```

### ตัวอย่างที่ 2: ทำงาน 1 เดือน (22 วัน)
```
วันทำงาน: 20 วัน (8 ชม./วัน)
OT รวม: 30 ชั่วโมง
วันลา: 2 วัน
อัตรา: 50 บาท/ชม.

คำนวณ:
- รายได้ทำงาน = 20 × 8 × 50 = 8,000 บาท
- รายได้ OT = 30 × 50 × 1.5 = 2,250 บาท
- รวมรายได้ = 10,250 บาท
- หักประกันสังคม (5%) = -512.50 บาท
- รายได้สุทธิ = 9,737.50 บาท
```

---

## 🛠️ การแก้ไขปัญหา

### ปัญหา: Login ไม่ได้
**แก้ไข:**
1. ตรวจสอบว่าเปิดใช้ Google Sign-in ใน Firebase Console
2. ตรวจสอบ Firebase Config ว่าถูกต้อง
3. ลองใช้ Incognito Mode
4. ตรวจสอบ Console (F12) ดู Error

### ปัญหา: บันทึกข้อมูลไม่ได้
**แก้ไข:**
1. ตรวจสอบ Database Rules ว่าถูกต้อง
2. ตรวจสอบว่า Login สำเร็จแล้ว
3. ดูที่ Firebase Console > Realtime Database ว่ามีข้อมูลหรือไม่
4. ตรวจสอบ Console (F12) ดู Error

### ปัญหา: ไม่เห็นข้อมูล
**แก้ไข:**
1. Refresh หน้าเว็บ
2. Logout แล้ว Login ใหม่
3. ตรวจสอบใน Firebase Console ว่ามีข้อมูลจริง

---

## 📱 การใช้งานบนมือถือ

ระบบนี้รองรับการใช้งานบนมือถือ:
- ✅ Responsive Design
- ✅ เปิดได้ทั้ง iOS และ Android
- ✅ UI ปรับตามขนาดหน้าจอ

---

## 🎨 การปรับแต่ง Theme

สามารถเปลี่ยนสีได้ที่ส่วน `:root` ในไฟล์ HTML:

```css
:root {
    --primary: #6366f1;      /* สีหลัก */
    --secondary: #8b5cf6;    /* สีรอง */
    --success: #10b981;      /* สีเขียว */
    --danger: #ef4444;       /* สีแดง */
    --warning: #f59e0b;      /* สีเหลือง */
}
```

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ Console (กด F12) ดู Error
2. ตรวจสอบ Firebase Console > Database > Rules
3. ตรวจสอบว่า Config ถูกต้อง

---

## ⚠️ ข้อควรระวัง

1. **อย่าแชร์ Firebase Config ให้ผู้อื่น** (มี API Key)
2. **ตั้ง Database Rules ให้ถูกต้อง** (ป้องกันการเข้าถึงโดยไม่ได้รับอนุญาต)
3. **สำรองข้อมูลสม่ำเสมอ** (Export จาก Firebase Console)
4. **ใช้ HTTPS เท่านั้น** เมื่อ Deploy

---

## 🚀 พร้อมใช้งานแล้ว!

เมื่อทำตามขั้นตอนครบแล้ว:
1. เปิดไฟล์ HTML ในเบราว์เซอร์
2. Login ด้วย Google
3. เริ่มบันทึกข้อมูลได้เลย!

**สนุกกับการใช้งาน! 🎉**
